(()=>{const C=SmartRadarConfig,$=id=>document.getElementById(id),R=new RadarCanvas($('radarCanvas'));const e={conn:$('connectionBadge'),dev:$('deviceBadge'),angle:$('angleValue'),dist:$('distanceValue'),fps:$('fpsValue'),rssi:$('rssiValue'),sensor:$('sensorValue'),servo:$('servoValue'),obj:$('objectsValue'),reads:$('readingsValue'),alarms:$('alarmsValue'),min:$('minDistanceValue'),max:$('maxDistanceValue'),up:$('uptimeValue'),ap:$('apIpValue'),sta:$('stationIpValue'),clients:$('apClientsValue'),heap:$('heapValue'),psram:$('psramValue'),cloud:$('cloudValue'),alert:$('radarAlert'),alertText:$('radarAlertText'),log:$('eventLog'),toast:$('toast'),mode:$('modeLabel')};const ids=['servoMinAngle','servoMaxAngle','servoStep','servoMoveIntervalMs','maxDistanceCm','alarmDistanceCm','buzzerEnabled','ledEnabled','alarmEnabled','staSsid','staPassword','cloudEnabled','cloudHost','cloudDeviceId','cloudToken'],f=Object.fromEntries(ids.map(i=>[i,$(i)]));let ws,retry=800,timer,csv,connected=false;e.mode.textContent=C.cloudMode?'Modo: Vercel / remoto':'Modo: ESP32 / local';const fmtB=v=>v>1048576?(v/1048576).toFixed(1)+' MB':v>1024?Math.round(v/1024)+' KB':'--',fmtD=v=>Number(v)>0?Math.round(v)+' cm':'--',fmtU=ms=>{let s=Math.floor((+ms||0)/1000),m=Math.floor(s/60),h=Math.floor(m/60),d=Math.floor(h/24);return d?`${d}d ${h%24}h`:h?`${h}h ${m%60}m`:m?`${m}m ${s%60}s`:`${s}s`};function event(msg,kind=''){const row=document.createElement('div');row.className='event '+kind;row.innerHTML=`<time>${new Date().toLocaleTimeString()}</time><span></span>`;row.querySelector('span').textContent=msg;e.log.prepend(row);while(e.log.children.length>120)e.log.lastElementChild.remove()}function toast(m){e.toast.textContent=m;e.toast.classList.remove('hidden');clearTimeout(toast.t);toast.t=setTimeout(()=>e.toast.classList.add('hidden'),3000)}function state(on){connected=on;e.conn.textContent=on?'WEBSOCKET CONNECTED':'WEBSOCKET DISCONNECTED';e.conn.className=on?'on':'off';if(!C.cloudMode)device(on)}function device(on){e.dev.textContent=on?'DISPOSITIVO ONLINE':'DISPOSITIVO OFFLINE';e.dev.className=on?'on':'warn'}function send(o){if(!ws||ws.readyState!==1){toast('WebSocket desconectado');return false}ws.send(JSON.stringify(o));return true}const cmd=c=>send({type:'command',command:c});function reconnect(){clearTimeout(timer);try{ws&&ws.close()}catch{}retry=800;connect()}function connect(){try{ws=new WebSocket(C.websocketUrl())}catch{return schedule()}ws.onopen=()=>{retry=800;state(true);event('WEBSOCKET CONNECTED','ok');if(C.cloudMode)cmd('get_config')};ws.onclose=()=>{state(false);if(C.cloudMode)device(false);event('WEBSOCKET DISCONNECTED','alert');schedule()};ws.onmessage=x=>{try{handle(JSON.parse(x.data))}catch{}}}function schedule(){clearTimeout(timer);timer=setTimeout(connect,retry);retry=Math.min(6000,retry*1.6)}function handle(m){if(m.type==='relay'){device(m.deviceOnline);e.cloud.textContent=m.redisBackplane?'REDIS + WSS':'WSS'}else if(m.type==='radar'){device(true);R.addReading(m);e.angle.textContent=Math.round(m.angle);e.dist.textContent=m.valid?Math.round(m.distance):'--';e.rssi.textContent=m.rssi?m.rssi+' dBm':'--';e.sensor.textContent=m.valid?'OK':'SENSOR ERROR';if(m.alarm){e.alert.classList.remove('hidden');e.alertText.textContent=`${Math.round(m.distance)} cm • ${Math.round(m.angle)}°`;if(m.event==='OBJECT_DETECTED')event(`⚠ Objeto: ${Math.round(m.distance)} cm em ${Math.round(m.angle)}°`,'alert')}else e.alert.classList.add('hidden')}else if(m.type==='status'){device(true);e.obj.textContent=m.objectsDetected??0;e.reads.textContent=m.readings??0;e.alarms.textContent=m.alarms??0;e.min.textContent=fmtD(m.minDistance);e.max.textContent=fmtD(m.maxDistance);e.up.textContent=fmtU(m.uptime);e.ap.textContent=m.apIp||'--';e.sta.textContent=m.stationIp||'--';e.clients.textContent=m.apClients??0;e.heap.textContent=fmtB(m.freeHeap);e.psram.textContent=fmtB(m.freePsram);e.sensor.textContent=m.sensorOk?'OK':'SENSOR ERROR';e.servo.textContent=m.running?'VARRENDO':'PARADO';e.cloud.textContent=m.cloudConnected?'ONLINE':'OFFLINE'}else if(m.type==='config')apply(m.config||{});else if(m.type==='ack'){toast(m.message||'OK');event(m.message||'OK','ok')}else if(m.type==='error'){toast(m.message||'Erro');event(m.message||'Erro','alert')}else if(m.type==='csv_begin')csv={id:m.transferId,data:''};else if(m.type==='csv_chunk'&&csv&&csv.id===m.transferId)csv.data+=m.data||'';else if(m.type==='csv_end'&&csv&&csv.id===m.transferId){download(csv.data);csv=null}}function apply(v){for(const id of ['servoMinAngle','servoMaxAngle','servoStep','servoMoveIntervalMs','maxDistanceCm','alarmDistanceCm','staSsid','cloudHost','cloudDeviceId','cloudToken'])if(v[id]!=null)f[id].value=v[id];for(const id of ['buzzerEnabled','ledEnabled','alarmEnabled','cloudEnabled'])f[id].checked=!!v[id];f.staPassword.value='';R.setMaxDistance(v.maxDistanceCm);if(v.cloudDeviceId&&v.cloudToken)C.saveCloudIdentity(v.cloudDeviceId,v.cloudToken)}function download(content){const n=new Date(),p=v=>String(v).padStart(2,'0'),name=`radar_logs_${n.getFullYear()}-${p(n.getMonth()+1)}-${p(n.getDate())}_${p(n.getHours())}-${p(n.getMinutes())}-${p(n.getSeconds())}.csv`,url=URL.createObjectURL(new Blob([content],{type:'text/csv;charset=utf-8'})),a=document.createElement('a');a.href=url;a.download=name;document.body.append(a);a.click();a.remove();setTimeout(()=>URL.revokeObjectURL(url),1000);toast(name)}document.querySelectorAll('[data-command]').forEach(b=>b.onclick=()=>cmd(b.dataset.command));$('clearRadarButton').onclick=()=>{R.clear();cmd('clear_radar')};$('exportButton').onclick=()=>cmd('export_logs');$('clearLogsButton').onclick=()=>confirm('Tem certeza que deseja apagar todos os logs?')&&cmd('clear_logs');$('clearEventsButton').onclick=()=>e.log.innerHTML='';$('reconnectCloudButton').onclick=()=>{C.saveCloudIdentity(f.cloudDeviceId.value.trim(),f.cloudToken.value.trim());reconnect()};$('settingsForm').onsubmit=ev=>{ev.preventDefault();const s={servoMinAngle:+f.servoMinAngle.value,servoMaxAngle:+f.servoMaxAngle.value,servoStep:+f.servoStep.value,servoMoveIntervalMs:+f.servoMoveIntervalMs.value,maxDistanceCm:+f.maxDistanceCm.value,alarmDistanceCm:+f.alarmDistanceCm.value,buzzerEnabled:f.buzzerEnabled.checked,ledEnabled:f.ledEnabled.checked,alarmEnabled:f.alarmEnabled.checked,staSsid:f.staSsid.value.trim(),cloudEnabled:f.cloudEnabled.checked,cloudHost:f.cloudHost.value.trim(),cloudDeviceId:f.cloudDeviceId.value.trim(),cloudToken:f.cloudToken.value.trim()};if(f.staPassword.value)s.staPassword=f.staPassword.value;if(s.servoMinAngle>=s.servoMaxAngle||s.alarmDistanceCm>s.maxDistanceCm)return toast('Confira limites de ângulo e distância');if(send({type:'settings',settings:s})){C.saveCloudIdentity(s.cloudDeviceId,s.cloudToken);R.setMaxDistance(s.maxDistanceCm)}};window.addEventListener('radar-fps',x=>e.fps.textContent=x.detail);f.cloudDeviceId.value=C.deviceId;f.cloudToken.value=C.token;state(false);device(!C.cloudMode);connect();setInterval(()=>connected&&send({type:'ping'}),12000)})();
