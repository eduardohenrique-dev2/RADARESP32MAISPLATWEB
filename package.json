{
  "name": "smart-radar-station",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build-web.mjs",
    "check:web": "node --check api/ws.js && node --check data/config.js && node --check data/radar.js && node --check data/app.js"
  },
  "engines": { "node": ">=20" },
  "dependencies": { "ioredis": "6.0.0", "ws": "8.21.3" }
}
